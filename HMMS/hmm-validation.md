1. Generate a new hmm for each hmm without the normalization, then all of the trajectories with the normalization, and see if the state assignments generally agree.
2. Previously a new HMM would be generated with each trajectory
3. find bulk measurements that we can use to validate the RD model as well